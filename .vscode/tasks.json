{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "makeflexpart",
            "type": "shell",
            "command": "make ncf=yes",
            "options": {
                "cwd": "${workspaceRoot}"
            }
        },
        
        {
            "label": "runflexpart",
            "type": "shell",
            "command": "${workspaceRoot}/FLEXPART pathnames",
            "options": {
                "cwd": "/home/zhendong/flexpart"
            }
        }
    ]
}